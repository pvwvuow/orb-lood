# =============================================================
# coturn configuration for orblood.ir
# Copy this file to /etc/turnserver.conf on your server
# =============================================================

# --- Listening ports ---
# We bind on BOTH 3478 (standard) and 443 (bypass ISP blocks).
# Iranian ISPs often block 3478 but never block 443 because it's HTTPS.
# Since ArvanCloud handles HTTPS for the website, nginx only needs
# port 80 — leaving 443 free for coturn.
listening-port=3478
listening-port=443
listening-ip=0.0.0.0

# --- Your server's public IP ---
# Replace with actual public IP of orblood.ir
# Find it with: curl ifconfig.me
external-ip=194.60.231.226

# --- Realm ---
realm=orblood.ir

# --- Authentication ---
lt-cred-mech
user=orblood:7a6c6c0bab5fb55ec1aa88c388daed13

# --- Relay port range (must be open in firewall) ---
min-port=49152
max-port=65535

# --- Logging ---
log-file=/var/log/turnserver.log
simple-log
# verbose

# --- Security ---
# Block relay to private/internal networks
no-multicast-peers
no-loopback-peers
denied-peer-ip=10.0.0.0-10.255.255.255
denied-peer-ip=172.16.0.0-172.31.255.255
denied-peer-ip=192.168.0.0-192.168.255.255
denied-peer-ip=127.0.0.0-127.255.255.255

# --- Performance ---
total-quota=100
stale-nonce=600

# --- Misc ---
fingerprint
no-cli
